{
    "name": "@sketches/caves",
    "private": true,
    "sketch": {
        "title": "Procedural Generation - Caves",
        "tags": ["procedural-generation"],
        "dev": {
            "secure": false
        }
    },
    "scripts": {
        "dev": "bun run ../../../dev/static-server.ts ./src",
        "build": "rm -rf ./dist && mkdir dist && cp src/* dist/"
    }
}
